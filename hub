INSERT INTO <hub>
SELECT DISTINCT 
	<HASH KEY>
	? BATCH_ID,
	LOAD_DATETIME,
	RECORD_SOURCE,
	<BUSINESS KEY>
FROM <stage>
WHERE NOT EXISTS (
	SELECT 1
	FROM <hub>
	WHERE <hub>.<BUSINESS KEY> = <stage>.<BUSINESS KEY>
)
AND LOAD_DATETIME = '${LOAD_DATETIME}'
